{
  "name": "omniclaw-agent",
  "version": "1.1.0",
  "description": "Agent locale con esecuzione reale di codice, memoria a grafo con embedding hash-based e cosine similarity, navigazione web via fetch reale, e canali WhatsApp/Telegram/Discord reali.",
  "main": "server.ts",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts",
    "build": "bun build server.ts --target=bun --outdir dist",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "ai-agent",
    "openclaw",
    "browser-use",
    "smolagents",
    "mem0",
    "deepseek-r1",
    "claude-code",
    "autonomous-agent"
  ],
  "author": "Giuseppe Lobbene",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
